/**
 * Block Manager - manages editor blocks and content structure
 */

export class BlockManager {
  // Future: Add block management functionality
  // This will handle different block types (paragraph, heading, etc.)
}

